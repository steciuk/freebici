import React from 'react';

const Navbar = () => {
  return (
    <nav
      css={{
        height: '3rem',
        backgroundColor: 'green',
      }}
    >
      Navbar
    </nav>
  );
};

export default Navbar;
